/**
 * Requests are the foundation on connecting, parsing, and handling information from the API. The core module handles
 * PublicRequests which are provided through the public API.
 *
 * There are implementations in extensions of the core module to handle different types of requests.
 */
package co.bitsquared.btceparser.core.requests;